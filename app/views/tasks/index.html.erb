<h1>My task</h1>
<% @tasks.each do |task| %>
<h2><%= link_to task.title, task_path(task.id) %></h2>
<input type="checkbox">
<p><%= link_to "Delete", task_delete_path(task.id), method: :delete, data: {confirm: 'Are you sure?'} %></p>
<% end %>
